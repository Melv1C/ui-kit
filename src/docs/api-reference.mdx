import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Documentation/API Reference" />

# API Reference

Référence complète des composants, hooks et utilitaires.

---

## Composants Base

### Button

```tsx
import { Button } from "@melv1c/ui-kit";

export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?:
    | "default"
    | "secondary"
    | "destructive"
    | "outline"
    | "ghost"
    | "link";
  size?: "default" | "sm" | "lg";
  asChild?: boolean;
}
```

**Utilisation** :

```tsx
<Button onClick={handleClick}>Click me</Button>
<Button variant="outline" size="sm">Small Outline</Button>
<Button disabled>Disabled</Button>
```

---

### Input

```tsx
import { Input } from "@melv1c/ui-kit";

export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  // Hérite de tous les attributs HTML
}
```

**Utilisation** :

```tsx
<Input placeholder="Your email" type="email" />
<Input disabled />
<Input className="custom-class" />
```

---

### Label

```tsx
import { Label } from "@melv1c/ui-kit";

export interface LabelProps extends React.LabelHTMLAttributes<HTMLLabelElement> {
  // Hérite de tous les attributs HTML
}
```

**Utilisation** :

```tsx
<Label htmlFor="email">Email Address</Label>
<Input id="email" />
```

---

### Card

```tsx
import { Card } from "@melv1c/ui-kit";

export interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  // Hérite de tous les attributs HTML
}
```

**Composition** :

```tsx
<Card>
  <Card.Header>
    <Card.Title>Title</Card.Title>
    <Card.Description>Description</Card.Description>
  </Card.Header>
  <Card.Content>Content</Card.Content>
  <Card.Footer>Footer</Card.Footer>
</Card>
```

---

## Composants Dialog

### Dialog

```tsx
import { Dialog } from "@melv1c/ui-kit";

export interface DialogProps {
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  children?: React.ReactNode;
}
```

**Composition** :

```tsx
<Dialog open={open} onOpenChange={setOpen}>
  <Dialog.Trigger asChild>
    <Button>Open</Button>
  </Dialog.Trigger>
  <Dialog.Content>
    <Dialog.Header>
      <Dialog.Title>Dialog Title</Dialog.Title>
    </Dialog.Header>
    <Dialog.Body>Content</Dialog.Body>
    <Dialog.Footer>
      <Button onClick={() => setOpen(false)}>Close</Button>
    </Dialog.Footer>
  </Dialog.Content>
</Dialog>
```

---

### Alert Dialog

```tsx
import { AlertDialog } from "@melv1c/ui-kit";

export interface AlertDialogProps extends DialogProps {
  // Même interface que Dialog
}
```

**Utilisation** :

```tsx
<AlertDialog>
  <AlertDialog.Trigger asChild>
    <Button variant="destructive">Delete</Button>
  </AlertDialog.Trigger>
  <AlertDialog.Content>
    <AlertDialog.Header>
      <AlertDialog.Title>Are you sure?</AlertDialog.Title>
    </AlertDialog.Header>
    <AlertDialog.Footer>
      <AlertDialog.Action>Delete</AlertDialog.Action>
      <AlertDialog.Cancel>Cancel</AlertDialog.Cancel>
    </AlertDialog.Footer>
  </AlertDialog.Content>
</AlertDialog>
```

---

## Composants Menu

### Dropdown Menu

```tsx
import { DropdownMenu } from "@melv1c/ui-kit";

export interface DropdownMenuProps {
  children?: React.ReactNode;
}
```

**Composition** :

```tsx
<DropdownMenu>
  <DropdownMenu.Trigger asChild>
    <Button variant="outline">Menu</Button>
  </DropdownMenu.Trigger>
  <DropdownMenu.Content>
    <DropdownMenu.Item>Option 1</DropdownMenu.Item>
    <DropdownMenu.Item>Option 2</DropdownMenu.Item>
    <DropdownMenu.Separator />
    <DropdownMenu.Item variant="destructive">Delete</DropdownMenu.Item>
  </DropdownMenu.Content>
</DropdownMenu>
```

---

### Context Menu

```tsx
import { ContextMenu } from "@melv1c/ui-kit";

export interface ContextMenuProps extends DropdownMenuProps {
  // Même interface que DropdownMenu
}
```

**Utilisation** :

```tsx
<ContextMenu>
  <ContextMenu.Trigger>
    <div>Right-click me</div>
  </ContextMenu.Trigger>
  <ContextMenu.Content>
    <ContextMenu.Item>Copy</ContextMenu.Item>
    <ContextMenu.Item>Paste</ContextMenu.Item>
  </ContextMenu.Content>
</ContextMenu>
```

---

## Composants Display

### Badge

```tsx
import { Badge } from "@melv1c/ui-kit";

export interface BadgeProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: "default" | "secondary" | "destructive" | "outline";
}
```

**Utilisation** :

```tsx
<Badge>New</Badge>
<Badge variant="secondary">Draft</Badge>
<Badge variant="destructive">Urgent</Badge>
```

---

### Avatar

```tsx
import { Avatar } from "@melv1c/ui-kit";

export interface AvatarProps {
  src?: string;
  alt?: string;
  fallback?: string;
}
```

**Utilisation** :

```tsx
<Avatar src="/avatar.jpg" alt="User" />
<Avatar fallback="JD" />
```

---

### Alert

```tsx
import { Alert } from "@melv1c/ui-kit";

export interface AlertProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: "default" | "destructive";
}
```

**Composition** :

```tsx
<Alert>
  <Alert.Title>Alert Title</Alert.Title>
  <Alert.Description>Alert message</Alert.Description>
</Alert>
```

---

## Composants Form

### Form

```tsx
import { Form } from "@melv1c/ui-kit";

export interface FormProps extends React.FormHTMLAttributes<HTMLFormElement> {
  // Hérite de tous les attributs HTML
}
```

**Utilisation** :

```tsx
<Form onSubmit={handleSubmit}>
  <div className="space-y-4">
    <div>
      <Label>Email</Label>
      <Input name="email" type="email" />
    </div>
    <Button type="submit">Submit</Button>
  </div>
</Form>
```

---

### Field

```tsx
import { Field } from "@melv1c/ui-kit";

export interface FieldProps {
  label?: string;
  error?: string;
  required?: boolean;
  children?: React.ReactNode;
}
```

**Utilisation** :

```tsx
<Field label="Email" error="Invalid email" required>
  <Input type="email" />
</Field>
```

---

## Hooks

### useTranslation

```tsx
import { useTranslation } from "react-i18next";

export function useTranslation(namespace?: string) {
  return {
    t: (key: string) => string, // Translation function
    i18n: I18nInstance, // i18n instance
    ready: boolean, // Ready state
  };
}
```

**Utilisation** :

```tsx
const { t, i18n } = useTranslation("ui");

return (
  <>
    <h1>{t("page.title")}</h1>
    <button onClick={() => i18n.changeLanguage("fr")}>Français</button>
  </>
);
```

---

### useMobile

```tsx
import { useMobile } from "@melv1c/ui-kit/hooks";

export function useMobile(): boolean {
  // Retourne true si la largeur est < 768px
}
```

**Utilisation** :

```tsx
const isMobile = useMobile();

return <div>{isMobile ? "Mobile view" : "Desktop view"}</div>;
```

---

## Utilitaires

### cn (Class Names)

```tsx
import { cn } from "@melv1c/ui-kit/lib/utils";

const className = cn(
  "px-2 py-1",
  isActive && "bg-blue-500",
  isDisabled && "opacity-50",
);
```

---

## Locales

### Traductions disponibles

```tsx
import { en, fr } from "@melv1c/ui-kit/locales";

// Type
interface Locale {
  button: Record<string, string>;
  dialog: Record<string, string>;
  form: Record<string, string>;
  // ... plus d'autres namespaces
}
```

---

## Tailwind Preset

### Usage

```tsx
import { uiKitPreset } from "@melv1c/ui-kit/tailwind.preset.js";

export default {
  presets: [uiKitPreset],
};
```

Cela ajoute :

- Variables CSS pour les couleurs
- Thèmes clair/sombre
- Extensions de spacing
- Configurations personnalisées

---

## Types

### Exporter les types

```tsx
import type {
  ButtonProps,
  CardProps,
  DialogProps,
  InputProps,
  LabelProps,
  // ... plus d'autres types
} from "@melv1c/ui-kit";
```

---

## Exports nommés

Tous les composants peuvent être importés nommément :

```tsx
import {
  Button,
  Card,
  Dialog,
  Input,
  Label,
  Badge,
  Avatar,
  Alert,
  // ... plus de composants
} from "@melv1c/ui-kit";
```

---

## Prochaines étapes

- 📚 [Component Guide](/?path=/docs/documentation-component-guide--docs) pour les patterns
- 🎨 [Theming](/?path=/docs/documentation-theming--docs) pour personnaliser
- 🌍 [Internationalisation](/?path=/docs/documentation-internationalisation--docs) pour i18n
